/**
 * Barrel re-export — sync implementation lives in `./sync` (background intervals + DataService).
 * Import from here or `../services/sync` interchangeably.
 */
export { syncService } from './sync';
